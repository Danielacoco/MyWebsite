
<h3> Comments </h3>
<% @pic.comments.each do |comment| %>
	<div class = "well">
		<strong><%= User.find((comment.user_id)).username if comment.user_id? %></strong> <br>
		<%= comment.user_id %>
	</div>
<%end%>